#!/bin/bash -l

#$ -P rise-grid          # project
#$ -N train              # job name
#$ -j y                  # merge stdout/stderr
#$ -o train.qlog         # output log
#$ -l h_rt=12:00:00      # walltime, adjust as needed
#$ -pe omp 8             # CPU cores, matches num_workers in train.py
#$ -l gpus=1             # request 1 GPU
#$ -l gpu_c=7.0          # min GPU compute capability
#$ -l gpu_type=A100      # request a specific GPU model, see `qgpus` for options

module load python3/3.10.12
module load pytorch/1.13.1  # provides torch/torchvision/torchaudio + matching CUDA, avoids per-job pip install of multi-GB nvidia/triton wheels

cd "$SGE_O_WORKDIR"
pip install --user -r requirements.txt

python src/training/train.py
